"""
Revista dels Castells WordPress helpers (local package).

This is not the PyPI ``models`` package — do not ``pip install models``.
Import submodules explicitly, e.g. ``from revista_wp.wp_api import make_session``.
"""
